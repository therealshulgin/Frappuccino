package rs.readahead.washington.mobile.util

/**
 * Phase 6.1.16 — onboarding Intent-extra keys, relocated from the removed
 * `tella-locking-ui` module (were `com.hzontal.tella_locking_ui.IS_ONBOARD_LOCK_SET`
 * and `IS_FROM_SETTINGS`). The String values are kept identical to the originals
 * so any in-flight Intent semantics are unchanged.
 */
const val IS_ONBOARD_LOCK_SET = "IS_ONBOARD_LOCK_SET"
const val IS_FROM_SETTINGS = "IS_FROM_SETTINGS"
